import { Routes } from '@angular/router';
import { SentenceComponent } from './sentence.component';


export const sentenceRoutes: Routes = [
    {
        path: 'sentence',
        component: SentenceComponent
    }
]